# Copy to scripts/ankr-local.env (gitignored) — never commit the real key.
ANKR_API_KEY=your_ankr_project_api_key_here

# Default smoke test = production-6 four chains (Ethereum, Base, Polygon, Arbitrum).
# ANKR_TEST_PROFILE=all   # 8-chain legacy stress test
# ANKR_LOG_RANGES=10,100,1000,3000   # only when profile=all
# ANKR_REQUEST_DELAY_MS=150
